<?php
/**
 * array_keys($arr) -  возвращает ключи массива
 */

    $arr = range(a,z,1);
    $arrflip = array_flip($arr);
    $arrkey = array_keys($arri);

    foreach($arrk as $value){
        echo $value.', ';
    }